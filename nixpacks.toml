[phases.setup]
nixPkgs = ["python311", "postgresql_16.dev", "gcc"]

[phases.install]
cmds = [
  "python -m venv --copies /opt/venv && /opt/venv/bin/pip install --no-cache-dir -c constraints.txt -r requirements.txt"
]
