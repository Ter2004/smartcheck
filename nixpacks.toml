[phases.setup]
nixPkgs = ["python311", "postgresql_16.dev", "gcc"]

[phases.install]
cmds = [
  "python -m venv --copies /opt/venv && /opt/venv/bin/pip install -r requirements.txt && /opt/venv/bin/pip uninstall -y opencv-python && /opt/venv/bin/pip install 'opencv-python-headless>=4.8.0'"
]
