# cache-bust: 2026-04-19
--extra-index-url https://download.pytorch.org/whl/cpu

flask==3.1.0
supabase==2.13.0
python-dotenv==1.1.0
deepface==0.0.93
gunicorn==23.0.0
python-dateutil==2.9.0
opencv-python-headless>=4.8.0
numpy==1.26.4
tf-keras
Flask-Session==0.8.0
Flask-Limiter==3.9.0
APScheduler==3.10.4
Flask-SQLAlchemy>=3.1.1
psycopg2-binary>=2.9.9
onnxruntime>=1.16.0

# CPU-only PyTorch for DeepFace anti_spoofing (Fasnet / MiniVision Silent-Face)
# Default torch from PyPI includes CUDA libs (~1.2GB) — we only need CPU.
# --extra-index-url at the top ensures pip falls back to PyPI for other packages.
torch==2.2.2+cpu
